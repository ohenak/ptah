# Artifact Commits (Phase 4)

Automatic git commit workflow for skill-produced artifacts. After a skill completes work in a worktree, changes are committed with a `[ptah]` prefix, merged back to the main branch, and the worktree is cleaned up.
