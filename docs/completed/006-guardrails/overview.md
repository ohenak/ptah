# Guardrails (Phase 6)

Safety and resource controls for the orchestrator. Covers token budget limits, max turns per thread, invocation timeouts, retry policies, and error handling boundaries to prevent runaway agent loops.
