# Init (Phase 1)

Project scaffolding and configuration. The `ptah init` command generates `ptah.config.json` with Discord, agent, orchestrator, git, and docs settings. Validates environment prerequisites (Node.js, Git, Discord bot token).
